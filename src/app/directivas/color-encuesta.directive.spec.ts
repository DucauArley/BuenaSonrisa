import { ColorEncuestaDirective } from './color-encuesta.directive';

describe('ColorEncuestaDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorEncuestaDirective();
    expect(directive).toBeTruthy();
  });
});
