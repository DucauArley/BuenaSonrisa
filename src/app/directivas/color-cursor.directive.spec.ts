import { ColorCursorDirective } from './color-cursor.directive';

describe('ColorCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCursorDirective();
    expect(directive).toBeTruthy();
  });
});
